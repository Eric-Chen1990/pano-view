---
"@ericchen1990/pano-view": minor
---

Add `previewUrl` to `Sphere` and `SphereScene` so a low-resolution 2:1 image can display while the full source loads. Relative paths resolve against the directory of `src`.
